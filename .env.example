# Frontend API base URL for Vite dev server / build
VITE_API_BASE_URL=http://localhost:8080/api/v1

# Optional demo credentials for automatic login during local development
VITE_DEMO_PHONE=13800000000
VITE_DEMO_PASSWORD=blindbox123

# Docker Compose defaults
MYSQL_ROOT_PASSWORD=blindbox
MYSQL_DATABASE=blindbox
AUTH_JWT_SECRET=super-secret-demo-key

# SMS gateway (configure when connecting to a real provider)
SMS_PROVIDER=mock
SMS_ENDPOINT=
SMS_API_KEY=
SMS_API_SECRET=
SMS_SIGN_NAME=
SMS_TEMPLATE_ID=

# Logging
LOG_LEVEL=info
